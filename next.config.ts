import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Foundation: no additional configuration required yet.
  // Database, media, and other integrations will be configured in later phases.
};

export default nextConfig;
